import { Route } from '@angular/router';

import RegisterSimpleComponent from './register.component';

const registerSimpleRoute: Route = {
  path: 'register-simple',
  component: RegisterSimpleComponent,
  title: 'Registration',
};

export default registerSimpleRoute;
